import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    error: {
      main: "#00f",
      light: "#ef5350",
    },
  },
  typography: {
    fontFamily: "Open Sans",
    fontSize: 16,
  },
});
